version: 0.2


phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - npm i
      # - npm run lint

  build:
    commands:
      - npm run build

artifacts:
  files:
    - "**/*"
  name: "web-dist"
  base-directory: build

cache:
  paths:
    - /root/.npm/**/*